<% stats = data.students[locals[:name]] %>

<section class="container-fluid">
  <div class="row-fluid">
    <div class="col-sm-12 col-md-10 col-md-offset-1 col-lg-8 col-lg-offet-2">
      <h2>Section 5: Portfolio Project</h2>
      <p>The vast majority of the required project work was the many components of building a comprehensive online portfolio for self-promotion.</p>
      <hr />
      <% data.portfolio.each do |project, details| %>
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title"><%= details.name %></h3>
        </div>
        <div class="panel-body">
          <!-- Expect this description to be enclosed in the necessary elements -->
          <%= details.info %>
          <div class="grades">
            <% if details.grades %>
            <% details.grades.each do |grade, title| %>
            <div class="alert alert-<%= stats.portfolio[project][grade].alert %>">
              <b><%= title %></b>
              <span class="pull-right"><%= stats.portfolio[project][grade].value %></span>
            </div>
            <% end %>
            <% else %>
            <div class="well">No grade given.</div>
            <% end %>
          </div>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</section>